--- a/purpur-server/build.gradle.kts
+++ b/purpur-server/build.gradle.kts
@@ -35,6 +_,24 @@
     activeFork = purpur
     // Purpur end - Rebrand
 
+    // ShreddedPaper start - Rebrand
+    val shreddedpaper = forks.register("shreddedpaper") {
+        forks = purpur
+        upstream.patchRepo("paperServer") {
+            upstreamRepo = purpur.patchedRepo("paperServer")
+            patchesDir = rootDirectory.dir("shreddedpaper-server/paper-patches")
+            outputDir = rootDirectory.dir("paper-server")
+        }
+        upstream.patchDir("purpurServer") {
+            upstreamPath = "purpur-server"
+            excludes = setOf("src/minecraft", "paper-patches", "minecraft-patches", "build.gradle.kts", "build.gradle.kts.patch")
+            patchesDir = rootDirectory.dir("shreddedpaper-server/purpur-patches")
+            outputDir = rootDirectory.dir("purpur-server")
+        }
+    }
+    activeFork = shreddedpaper
+    // ShreddedPaper e d - Rebrand
+
     spigot {
         enabled = true
         buildDataRef = "17f77cee7117ab9d6175f088ae8962bfd04e61a9"
@@ -122,10 +_,14 @@
     main {
         java { srcDir("../paper-server/src/main/java") }
         resources { srcDir("../paper-server/src/main/resources") }
+        java { srcDir("../purpur-server/src/main/java") } // ShreddedPaper - Rebrand
+        resources { srcDir("../purpur-server/src/main/resources") } // ShreddedPaper - Rebrand
     }
     test {
         java { srcDir("../paper-server/src/test/java") }
         resources { srcDir("../paper-server/src/test/resources") }
+        java { srcDir("../purpur-server/src/test/java") } // ShreddedPaper - Rebrand
+        resources { srcDir("../purpur-server/src/test/resources") } // ShreddedPaper - Rebrand
     }
 }
 val log4jPlugins = sourceSets.create("log4jPlugins") {
@@ -154,7 +_,8 @@
 }
 
 dependencies {
-    implementation(project(":purpur-api")) // Purpur
+    implementation(project(":shreddedpaper-api")) // Purpur // ShreddedPaper
     implementation("ca.spottedleaf:concurrentutil:0.0.8")
+    implementation("org.jctools:jctools-core:4.0.6") // ShreddedPaper - bounded region mailboxes
     implementation("org.jline:jline-terminal-ffm:3.27.1") // use ffm on java 22+
     implementation("org.jline:jline-terminal-jni:3.27.1") // fall back to jni on java 21
@@ -175,10 +_,13 @@
+    implementation(platform("io.netty:netty-bom:4.2.12.Final")) // ShreddedPaper - align Netty 4.2 patch line
-    implementation("com.velocitypowered:velocity-native:3.4.0-SNAPSHOT") {
+    implementation("com.velocitypowered:velocity-native:3.4.0") {
         isTransitive = false
     }
-    implementation("io.netty:netty-codec-haproxy:4.2.7.Final") // Add support for proxy protocol
+    implementation("io.netty:netty-codec-haproxy") // Add support for proxy protocol
     implementation("org.apache.logging.log4j:log4j-iostreams:2.24.1")
     implementation("org.ow2.asm:asm-commons:9.8")
     implementation("org.spongepowered:configurate-yaml:4.2.0")
+    implementation("com.github.luben:zstd-jni:1.5.7-7")
+    implementation("net.openhft:zero-allocation-hashing:0.16")
 
     implementation("org.mozilla:rhino-runtime:1.7.14") // Purpur
     implementation("org.mozilla:rhino-engine:1.7.14") // Purpur
@@ -229,14 +_,14 @@
         val gitBranch = git.exec(providers, "rev-parse", "--abbrev-ref", "HEAD").get().trim()
         attributes(
             "Main-Class" to "org.bukkit.craftbukkit.Main",
-            "Implementation-Title" to "Purpur", // Purpur
+            "Implementation-Title" to "Astatine", // Purpur // ShreddedPaper
             "Implementation-Version" to implementationVersion,
             "Implementation-Vendor" to date,
-            "Specification-Title" to "Purpur", // Purpur
+            "Specification-Title" to "Astatine", // Purpur // ShreddedPaper
             "Specification-Version" to project.version,
-            "Specification-Vendor" to "Purpur Team", // Purpur
-            "Brand-Id" to "purpurmc:purpur", // Purpur
-            "Brand-Name" to "Purpur", // Purpur
+            "Specification-Vendor" to "Astatine Team", // Purpur // ShreddedPaper
+            "Brand-Id" to "astatine:astatine", // Purpur // ShreddedPaper
+            "Brand-Name" to "Astatine", // Purpur // ShreddedPaper
             "Build-Number" to (build ?: ""),
             "Build-Time" to buildTime.toString(),
             "Git-Branch" to gitBranch,
