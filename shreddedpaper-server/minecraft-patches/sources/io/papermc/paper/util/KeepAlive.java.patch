--- a/io/papermc/paper/util/KeepAlive.java
+++ b/io/papermc/paper/util/KeepAlive.java
@@ -2,6 +_,8 @@
 
 public class KeepAlive {
 
+    private static final long NETWORK_KEEPALIVE_EXPIRATION_NS = java.util.concurrent.TimeUnit.SECONDS.toNanos(30L);
+
     public long lastKeepAliveTx = System.nanoTime();
     public static final record KeepAliveResponse(long txTimeNS, long rxTimeNS) {
         public long latencyNS() {
@@ -14,7 +_,25 @@
 
     public final PingCalculator pingCalculator1m = new PingCalculator(java.util.concurrent.TimeUnit.MINUTES.toNanos(1L));
     public final PingCalculator pingCalculator5s = new PingCalculator(java.util.concurrent.TimeUnit.SECONDS.toNanos(5L));
+    private final java.util.concurrent.ConcurrentMap<Long, Long> networkKeepAliveTxTimes = new java.util.concurrent.ConcurrentHashMap<>();
+    public final PingCalculator networkPingCalculator5s = new PingCalculator(java.util.concurrent.TimeUnit.SECONDS.toNanos(5L));
 
+    public void trackNetworkKeepAliveSent(long challengeId, long txTimeNS) {
+        this.networkKeepAliveTxTimes.entrySet().removeIf(entry -> txTimeNS - entry.getValue() > NETWORK_KEEPALIVE_EXPIRATION_NS);
+        this.networkKeepAliveTxTimes.put(challengeId, txTimeNS);
+    }
+
+    public void trackNetworkKeepAliveReceived(long challengeId, long rxTimeNS) {
+        Long txTimeNS = this.networkKeepAliveTxTimes.remove(challengeId);
+        if (txTimeNS != null && rxTimeNS >= txTimeNS) {
+            this.networkPingCalculator5s.update(new KeepAliveResponse(txTimeNS, rxTimeNS));
+        }
+    }
+
+    public int getNetworkLatencyMS() {
+        return this.networkPingCalculator5s.getAvgLatencyMS();
+    }
+
     public static final class PingCalculator {
 
         private final long intervalNS;
