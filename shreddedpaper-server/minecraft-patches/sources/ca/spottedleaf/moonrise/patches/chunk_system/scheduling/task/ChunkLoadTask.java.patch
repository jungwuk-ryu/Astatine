--- a/ca/spottedleaf/moonrise/patches/chunk_system/scheduling/task/ChunkLoadTask.java
+++ b/ca/spottedleaf/moonrise/patches/chunk_system/scheduling/task/ChunkLoadTask.java
@@ -11,6 +11,7 @@ import ca.spottedleaf.moonrise.patches.chunk_system.io.MoonriseRegionFileIO;
 import ca.spottedleaf.moonrise.patches.chunk_system.level.poi.PoiChunk;
 import ca.spottedleaf.moonrise.patches.chunk_system.scheduling.ChunkTaskScheduler;
 import ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder;
+import io.multipaper.shreddedpaper.threading.region.RegionChunkExecutorLimiter;
 import net.minecraft.core.registries.Registries;
 import net.minecraft.nbt.CompoundTag;
 import net.minecraft.server.level.ServerLevel;
@@ -294,7 +295,10 @@ public final class ChunkLoadTask extends ChunkProgressionTask {
 
         @Override
         protected PrioritisedExecutor.PrioritisedTask createOffMain(final Runnable run, final Priority priority) {
-            return this.scheduler.loadExecutor.createTask(run, priority);
+            return RegionChunkExecutorLimiter.createTask(
+                this.world, this.chunkX, this.chunkZ, this.scheduler.loadExecutor, run, priority,
+                RegionChunkExecutorLimiter.WorkType.LOAD_DECODE
+            );
         }
 
         @Override
@@ -391,7 +395,10 @@ public final class ChunkLoadTask extends ChunkProgressionTask {
 
         @Override
         protected PrioritisedExecutor.PrioritisedTask createOffMain(final Runnable run, final Priority priority) {
-            return this.scheduler.loadExecutor.createTask(run, priority);
+            return RegionChunkExecutorLimiter.createTask(
+                this.world, this.chunkX, this.chunkZ, this.scheduler.loadExecutor, run, priority,
+                RegionChunkExecutorLimiter.WorkType.LOAD_DECODE
+            );
         }
 
         @Override
@@ -453,7 +460,10 @@ public final class ChunkLoadTask extends ChunkProgressionTask {
 
         @Override
         protected PrioritisedExecutor.PrioritisedTask createOffMain(final Runnable run, final Priority priority) {
-            return this.scheduler.loadExecutor.createTask(run, priority);
+            return RegionChunkExecutorLimiter.createTask(
+                this.world, this.chunkX, this.chunkZ, this.scheduler.loadExecutor, run, priority,
+                RegionChunkExecutorLimiter.WorkType.LOAD_DECODE
+            );
         }
 
         @Override
