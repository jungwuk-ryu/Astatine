--- a/alternate/current/wire/LevelHelper.java
+++ b/alternate/current/wire/LevelHelper.java
@@ -1,5 +_,8 @@
 package alternate.current.wire;
 
+import io.multipaper.shreddedpaper.threading.ShreddedPaperTickThread;
+import io.multipaper.shreddedpaper.threading.ownership.ShreddedPaperAccess;
+import io.multipaper.shreddedpaper.threading.region.RegionTaskClass;
 import org.bukkit.craftbukkit.block.CraftBlock;
 import org.bukkit.event.block.BlockRedstoneEvent;
 
@@ -8,7 +_,6 @@
 import net.minecraft.world.level.block.Block;
 import net.minecraft.world.level.block.state.BlockState;
 import net.minecraft.world.level.chunk.ChunkAccess;
-import net.minecraft.world.level.chunk.status.ChunkStatus;
 import net.minecraft.world.level.chunk.LevelChunkSection;
 
 class LevelHelper {
@@ -37,7 +_,18 @@
         int z = pos.getZ();
         int index = level.getSectionIndex(y);
 
-        ChunkAccess chunk = level.getChunk(x >> 4, z >> 4, ChunkStatus.FULL, true);
+        if (ShreddedPaperTickThread.isShreddedPaperTickThread() && !ShreddedPaperAccess.isOwned(level, pos)) {
+            final io.multipaper.shreddedpaper.threading.ownership.OwnerTaskResult handoffResult = ShreddedPaperAccess.runWithWriteLockIfHeldOrDeferToOwner(level, pos, RegionTaskClass.OWNER_HANDOFF, () -> setWireState(level, pos, state, updateNeighborShapes));
+            if (handoffResult == io.multipaper.shreddedpaper.threading.ownership.OwnerTaskResult.REJECTED || handoffResult == io.multipaper.shreddedpaper.threading.ownership.OwnerTaskResult.CHUNK_UNAVAILABLE) {
+                throw new IllegalStateException("Failed to hand off cross-owner redstone wire write; world=" + level.getWorld().getName() + ", pos=" + pos + ", state=" + state);
+            }
+            return false;
+        }
+
+        ChunkAccess chunk = level.getChunkIfLoaded(pos);
+        if (chunk == null) {
+            return false;
+        }
         LevelChunkSection section = chunk.getSections()[index];
 
         if (section == null) {
