--- a/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
+++ b/net/minecraft/server/network/ServerCommonPacketListenerImpl.java
@@ -1,7 +_,12 @@
 package net.minecraft.server.network;
 
+import ca.spottedleaf.moonrise.common.util.TickThread;
 import com.mojang.authlib.GameProfile;
 import com.mojang.logging.LogUtils;
+import io.multipaper.shreddedpaper.ShreddedPaper;
+import io.multipaper.shreddedpaper.region.RegionPos;
+import io.multipaper.shreddedpaper.threading.ShreddedPaperTickThread;
+import io.multipaper.shreddedpaper.threading.SynchronousPluginExecution;
 import io.netty.channel.ChannelFutureListener;
 import net.minecraft.CrashReport;
 import net.minecraft.CrashReportCategory;
@@ -283,7 +_,7 @@
         this.disconnect(DISCONNECT_UNEXPECTED_QUERY, io.papermc.paper.connection.DisconnectionReason.INVALID_COOKIE); // Paper - kick event cause
     }
 
-    protected void keepConnectionAlive() {
+    public void keepConnectionAlive() { // ShreddedPaper - make public
         Profiler.get().push("keepAlive");
         long millis = Util.getMillis();
         // Paper start - improve keepalives
@@ -391,7 +_,15 @@
         if (this.processedDisconnect) {
             return;
         }
-        if (!this.cserver.isPrimaryThread()) {
+        // ShreddedPaper start - region workers must never wait for another owner while holding region locks
+        if (this instanceof ServerGamePacketListenerImpl serverGamePacketListener
+                && ShreddedPaperTickThread.isShreddedPaperTickThread()
+                && !TickThread.isTickThreadFor(serverGamePacketListener.player)) {
+            serverGamePacketListener.disconnectAsync(disconnectionDetails);
+            return;
+        }
+        // ShreddedPaper end - region workers must never wait for another owner while holding region locks
+        if (this instanceof ServerGamePacketListenerImpl serverGamePacketListener ? !TickThread.isTickThreadFor(serverGamePacketListener.player) : !this.cserver.isPrimaryThread()) { // ShreddedPaper
             org.bukkit.craftbukkit.util.Waitable waitable = new org.bukkit.craftbukkit.util.Waitable() {
                 @Override
                 protected Object evaluate() {
@@ -400,14 +_,31 @@
                 }
             };
 
-            this.server.processQueue.add(waitable);
+            // ShreddedPaper - run on player's thread
+            if (this instanceof ServerGamePacketListenerImpl serverGamePacketListener) {
+                if (TickThread.isTickThread()) {
+                    serverGamePacketListener.player.level().chunkSource.tickingRegions.execute(RegionPos.forChunk(serverGamePacketListener.player.chunkPosition()), waitable);
+                } else {
+                    ShreddedPaper.runSync(serverGamePacketListener.player, waitable); // this.server.processQueue.add(waitable);
+                }
+            } else {
+                this.server.processQueue.add(waitable);
+            }
+            // ShreddedPaper end - run on player's thread
 
             try {
-                waitable.get();
+                SynchronousPluginExecution.runWithoutCurrentPluginLocks(() -> { // ShreddedPaper - avoid plugin-lock deadlock while waiting for owner disconnect
+                    if (TickThread.isTickThread()) this.server.managedBlock(waitable::isDone); // ShreddedPaper - don't block when waiting
+                    waitable.get();
+                }); // ShreddedPaper - avoid plugin-lock deadlock while waiting for owner disconnect
             } catch (InterruptedException e) {
                 Thread.currentThread().interrupt();
             } catch (java.util.concurrent.ExecutionException e) {
                 throw new RuntimeException(e);
+            } catch (RuntimeException e) {
+                throw e;
+            } catch (Exception e) {
+                throw new RuntimeException(e);
             }
             return;
         }
@@ -497,6 +_,18 @@
 
     public int latency() {
         return this.latency;
+    }
+
+    public void recordNetworkKeepAliveSent(long challengeId, long txTimeNS) {
+        this.keepAlive.trackNetworkKeepAliveSent(challengeId, txTimeNS);
+    }
+
+    public void recordNetworkKeepAliveReceived(long challengeId, long rxTimeNS) {
+        this.keepAlive.trackNetworkKeepAliveReceived(challengeId, rxTimeNS);
+    }
+
+    public int networkLatency() {
+        return this.keepAlive.getNetworkLatencyMS();
     }
 
     protected CommonListenerCookie createCookie(ClientInformation clientInformation) {
