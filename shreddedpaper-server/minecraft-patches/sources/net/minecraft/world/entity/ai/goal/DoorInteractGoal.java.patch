--- a/net/minecraft/world/entity/ai/goal/DoorInteractGoal.java
+++ b/net/minecraft/world/entity/ai/goal/DoorInteractGoal.java
@@ -1,6 +_,7 @@
 package net.minecraft.world.entity.ai.goal;
 
 import net.minecraft.core.BlockPos;
+import ca.spottedleaf.moonrise.common.util.TickThread;
 import net.minecraft.world.entity.Mob;
 import net.minecraft.world.entity.ai.util.GoalUtils;
 import net.minecraft.world.level.block.DoorBlock;
@@ -27,8 +_,9 @@
         if (!this.hasDoor) {
             return false;
         } else {
-            BlockState blockState = this.mob.level().getBlockState(this.doorPos);
-            if (!(blockState.getBlock() instanceof DoorBlock)) {
+            if (!this.isOwnedDoorPos()) return false; // ShreddedPaper - avoid off-owner door access
+            BlockState blockState = this.mob.level().getBlockStateIfLoaded(this.doorPos); // ShreddedPaper - avoid sync chunk loads from door goals
+            if (blockState == null || !(blockState.getBlock() instanceof DoorBlock)) {
                 this.hasDoor = false;
                 return false;
             } else {
@@ -39,8 +_,9 @@
 
     protected void setOpen(boolean _open) {
         if (this.hasDoor) {
-            BlockState blockState = this.mob.level().getBlockState(this.doorPos);
-            if (blockState.getBlock() instanceof DoorBlock) {
+            if (!this.isOwnedDoorPos()) return; // ShreddedPaper - avoid off-owner door mutation
+            BlockState blockState = this.mob.level().getBlockStateIfLoaded(this.doorPos); // ShreddedPaper - avoid sync chunk loads from door goals
+            if (blockState != null && blockState.getBlock() instanceof DoorBlock) {
                 ((DoorBlock)blockState.getBlock()).setOpen(this.mob, this.mob.level(), blockState, this.doorPos, _open);
             }
         }
@@ -54,12 +_,13 @@
             return false;
         } else {
             Path path = this.mob.getNavigation().getPath();
-            if (path != null && !path.isDone()) {
+            if (path != null && path.isProcessed() && !path.isDone()) { // DivineMC - Async Pathfinding
                 for (int i = 0; i < Math.min(path.getNextNodeIndex() + 2, path.getNodeCount()); i++) {
                     Node node = path.getNode(i);
                     this.doorPos = new BlockPos(node.x, node.y + 1, node.z);
                     if (!(this.mob.distanceToSqr(this.doorPos.getX(), this.mob.getY(), this.doorPos.getZ()) > 2.25)) {
-                        this.hasDoor = DoorBlock.isWoodenDoor(this.mob.level(), this.doorPos);
+                        BlockState doorState = this.isOwnedDoorPos() ? this.mob.level().getBlockStateIfLoaded(this.doorPos) : null; // ShreddedPaper - avoid sync/off-owner door scans
+                        this.hasDoor = doorState != null && DoorBlock.isWoodenDoor(doorState);
                         if (this.hasDoor) {
                             return true;
                         }
@@ -67,7 +_,8 @@
                 }
 
                 this.doorPos = this.mob.blockPosition().above();
-                this.hasDoor = DoorBlock.isWoodenDoor(this.mob.level(), this.doorPos);
+                BlockState doorState = this.isOwnedDoorPos() ? this.mob.level().getBlockStateIfLoaded(this.doorPos) : null; // ShreddedPaper - avoid sync/off-owner door scans
+                this.hasDoor = doorState != null && DoorBlock.isWoodenDoor(doorState);
                 return this.hasDoor;
             } else {
                 return false;
@@ -100,5 +_,9 @@
         if (f2 < 0.0F) {
             this.passed = true;
         }
+    }
+
+    private boolean isOwnedDoorPos() {
+        return !(this.mob.level() instanceof net.minecraft.server.level.ServerLevel serverLevel) || TickThread.isTickThreadFor(serverLevel, this.doorPos);
     }
 }
