--- a/net/minecraft/world/entity/ai/goal/TryFindWaterGoal.java
+++ b/net/minecraft/world/entity/ai/goal/TryFindWaterGoal.java
@@ -1,9 +_,12 @@
 package net.minecraft.world.entity.ai.goal;
 
+import ca.spottedleaf.moonrise.common.util.TickThread;
 import net.minecraft.core.BlockPos;
+import net.minecraft.server.level.ServerLevel;
 import net.minecraft.tags.FluidTags;
 import net.minecraft.util.Mth;
 import net.minecraft.world.entity.PathfinderMob;
+import net.minecraft.world.level.material.FluidState;
 
 public class TryFindWaterGoal extends Goal {
     private final PathfinderMob mob;
@@ -14,7 +_,9 @@
 
     @Override
     public boolean canUse() {
-        return this.mob.onGround() && !this.mob.level().getFluidState(this.mob.blockPosition()).is(FluidTags.WATER);
+        if (this.mob.level() instanceof ServerLevel serverLevel && !TickThread.isTickThreadFor(serverLevel, this.mob.blockPosition())) return false; // ShreddedPaper - avoid off-owner water-finding goals
+        FluidState fluidState = this.mob.level().getFluidIfLoaded(this.mob.blockPosition()); // ShreddedPaper - avoid sync chunk loads during water-finding goals
+        return this.mob.onGround() && fluidState != null && !fluidState.is(FluidTags.WATER);
     }
 
     @Override
@@ -29,7 +_,9 @@
             this.mob.getBlockY(),
             Mth.floor(this.mob.getZ() + 2.0)
         )) {
-            if (this.mob.level().getFluidState(blockPos1).is(FluidTags.WATER)) {
+            if (this.mob.level() instanceof ServerLevel serverLevel && !TickThread.isTickThreadFor(serverLevel, blockPos1)) continue; // ShreddedPaper - avoid off-owner water-finding goals
+            FluidState fluidState = this.mob.level().getFluidIfLoaded(blockPos1); // ShreddedPaper - avoid sync chunk loads during water-finding goals
+            if (fluidState != null && fluidState.is(FluidTags.WATER)) {
                 blockPos = blockPos1;
                 break;
             }
