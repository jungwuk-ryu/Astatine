--- a/net/minecraft/world/entity/ai/behavior/ValidateNearbyPoi.java
+++ b/net/minecraft/world/entity/ai/behavior/ValidateNearbyPoi.java
@@ -4,2 +4,3 @@
 import java.util.function.Predicate;
+import ca.spottedleaf.moonrise.common.util.TickThread;
 import net.minecraft.core.BlockPos;
@@ -27,2 +28,3 @@
                 ServerLevel level1 = level.getServer().getLevel(globalPos.dimension());
+                if (level1 != null && !TickThread.isTickThreadFor(level1, blockPos)) return false; // ShreddedPaper - don't touch POI blocks outside this owner region
                 if (level1 == null || !level1.getPoiManager().exists(blockPos, poiValidator)) {
@@ -32,4 +34,4 @@
                     if (!bedIsOccupiedByVillager(level1, blockPos)) {
-                        level.getPoiManager().release(blockPos);
-                        level.debugSynchronizers().updatePoi(blockPos);
+                        level1.getPoiManager().release(blockPos);
+                        level1.debugSynchronizers().updatePoi(blockPos);
                     }
@@ -45,4 +47,4 @@
     private static boolean bedIsOccupied(ServerLevel level, BlockPos pos, LivingEntity entity) {
-        BlockState blockState = level.getBlockState(pos);
-        return blockState.is(BlockTags.BEDS) && blockState.getValue(BedBlock.OCCUPIED) && !entity.isSleeping();
+        BlockState blockState = level.getBlockStateIfLoaded(pos); // ShreddedPaper - avoid sync chunk loads while validating POIs
+        return blockState != null && blockState.is(BlockTags.BEDS) && blockState.getValue(BedBlock.OCCUPIED) && !entity.isSleeping();
     }
