--- a/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
+++ b/net/minecraft/world/entity/vehicle/boat/AbstractBoat.java
@@ -453,13 +_,13 @@
         for (int i = floor1; i < ceil1; i++) {
             float f = 0.0F;
 
-            for (int i1 = floor; i1 < ceil; i1++) {
-                for (int i2 = floor2; i2 < ceil2; i2++) {
-                    mutableBlockPos.set(i1, i, i2);
-                    FluidState fluidState = this.level().getFluidState(mutableBlockPos);
-                    if (fluidState.is(FluidTags.WATER)) {
-                        f = Math.max(f, fluidState.getHeight(this.level(), mutableBlockPos));
-                    }
+                for (int i1 = floor; i1 < ceil; i1++) {
+                    for (int i2 = floor2; i2 < ceil2; i2++) {
+                        mutableBlockPos.set(i1, i, i2);
+                        FluidState fluidState = this.level().getFluidIfLoaded(mutableBlockPos); // ShreddedPaper - avoid sync chunk loads from boat water scans
+                        if (fluidState != null && fluidState.is(FluidTags.WATER)) {
+                            f = Math.max(f, fluidState.getHeight(this.level(), mutableBlockPos));
+                        }
 
                     if (f >= 1.0F) {
                         continue label39;
@@ -496,7 +_,10 @@
                     for (int i10 = i2; i10 < i3; i10++) {
                         if (i9 <= 0 || i10 != i2 && i10 != i3 - 1) {
                             mutableBlockPos.set(i7, i10, i8);
-                            BlockState blockState = this.level().getBlockState(mutableBlockPos);
+                            BlockState blockState = this.level().getBlockStateIfLoaded(mutableBlockPos); // ShreddedPaper - avoid sync chunk loads from boat ground scans
+                            if (blockState == null) {
+                                continue;
+                            }
                             if (!(blockState.getBlock() instanceof WaterlilyBlock)
                                 && Shapes.joinIsNotEmpty(
                                     blockState.getCollisionShape(this.level(), mutableBlockPos).move(mutableBlockPos), voxelShape, BooleanOp.AND
@@ -510,7 +_,7 @@
             }
         }
 
-        return f / i6;
+        return i6 == 0 ? 0.0F : f / i6;
     }
 
     private boolean checkInWater() {
@@ -525,15 +_,15 @@
         this.waterLevel = -Double.MAX_VALUE;
         BlockPos.MutableBlockPos mutableBlockPos = new BlockPos.MutableBlockPos();
 
-        for (int i = floor; i < ceil; i++) {
-            for (int i1 = floor1; i1 < ceil1; i1++) {
-                for (int i2 = floor2; i2 < ceil2; i2++) {
-                    mutableBlockPos.set(i, i1, i2);
-                    FluidState fluidState = this.level().getFluidState(mutableBlockPos);
-                    if (fluidState.is(FluidTags.WATER)) {
-                        float f = i1 + fluidState.getHeight(this.level(), mutableBlockPos);
-                        this.waterLevel = Math.max((double)f, this.waterLevel);
-                        flag |= boundingBox.minY < f;
+            for (int i = floor; i < ceil; i++) {
+                for (int i1 = floor1; i1 < ceil1; i1++) {
+                    for (int i2 = floor2; i2 < ceil2; i2++) {
+                        mutableBlockPos.set(i, i1, i2);
+                        FluidState fluidState = this.level().getFluidIfLoaded(mutableBlockPos); // ShreddedPaper - avoid sync chunk loads from boat water scans
+                        if (fluidState != null && fluidState.is(FluidTags.WATER)) {
+                            float f = i1 + fluidState.getHeight(this.level(), mutableBlockPos);
+                            this.waterLevel = Math.max((double)f, this.waterLevel);
+                            flag |= boundingBox.minY < f;
                     }
                 }
             }
@@ -554,15 +_,15 @@
         boolean flag = false;
         BlockPos.MutableBlockPos mutableBlockPos = new BlockPos.MutableBlockPos();
 
-        for (int i = floor; i < ceil; i++) {
-            for (int i1 = floor1; i1 < ceil1; i1++) {
-                for (int i2 = floor2; i2 < ceil2; i2++) {
-                    mutableBlockPos.set(i, i1, i2);
-                    FluidState fluidState = this.level().getFluidState(mutableBlockPos);
-                    if (fluidState.is(FluidTags.WATER) && d < mutableBlockPos.getY() + fluidState.getHeight(this.level(), mutableBlockPos)) {
-                        if (!fluidState.isSource()) {
-                            return AbstractBoat.Status.UNDER_FLOWING_WATER;
-                        }
+            for (int i = floor; i < ceil; i++) {
+                for (int i1 = floor1; i1 < ceil1; i1++) {
+                    for (int i2 = floor2; i2 < ceil2; i2++) {
+                        mutableBlockPos.set(i, i1, i2);
+                        FluidState fluidState = this.level().getFluidIfLoaded(mutableBlockPos); // ShreddedPaper - avoid sync chunk loads from boat water scans
+                        if (fluidState != null && fluidState.is(FluidTags.WATER) && d < mutableBlockPos.getY() + fluidState.getHeight(this.level(), mutableBlockPos)) {
+                            if (!fluidState.isSource()) {
+                                return AbstractBoat.Status.UNDER_FLOWING_WATER;
+                            }
 
                         flag = true;
                     }
@@ -763,8 +_,11 @@
         if (!this.isPassenger()) {
             if (onGround) {
                 this.resetFallDistance();
-            } else if (!this.level().getFluidState(this.blockPosition().below()).is(FluidTags.WATER) && y < 0.0) {
+            } else {
+                FluidState belowFluidState = this.level().getFluidIfLoaded(this.blockPosition().below()); // ShreddedPaper - avoid sync chunk loads from boat fall checks
+                if ((belowFluidState == null || !belowFluidState.is(FluidTags.WATER)) && y < 0.0) {
                 this.fallDistance -= (float)y;
+                }
             }
         }
     }
