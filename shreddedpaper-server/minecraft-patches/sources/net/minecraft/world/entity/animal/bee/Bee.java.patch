--- a/net/minecraft/world/entity/animal/bee/Bee.java
+++ b/net/minecraft/world/entity/animal/bee/Bee.java
@@ -1,7 +_,9 @@
 package net.minecraft.world.entity.animal.bee;
 
+import ca.spottedleaf.moonrise.common.util.TickThread;
 import com.google.common.collect.Lists;
 import it.unimi.dsi.fastutil.longs.Long2LongOpenHashMap;
+import java.util.ArrayList;
 import java.util.Comparator;
 import java.util.EnumSet;
 import java.util.List;
@@ -9,6 +_,7 @@
 import java.util.stream.Collectors;
 import java.util.stream.Stream;
 import net.minecraft.core.BlockPos;
+import net.minecraft.core.SectionPos;
 import net.minecraft.core.particles.ParticleOptions;
 import net.minecraft.core.particles.ParticleTypes;
 import net.minecraft.network.syncher.EntityDataAccessor;
@@ -71,6 +_,7 @@
 import net.minecraft.world.item.BlockItem;
 import net.minecraft.world.item.ItemStack;
 import net.minecraft.world.item.enchantment.EnchantmentHelper;
+import net.minecraft.world.level.ChunkPos;
 import net.minecraft.world.level.Level;
 import net.minecraft.world.level.LevelReader;
 import net.minecraft.world.level.block.Block;
@@ -941,7 +_,7 @@
                         } else {
                             Bee.this.pathfindRandomlyTowards(Bee.this.hivePos);
                         }
-                    } else {
+                    } else if (navigation.getPath() != null && navigation.getPath().isProcessed()) { // DivineMC - check processing
                         boolean flag = this.pathfindDirectlyTowards(Bee.this.hivePos);
                         if (!flag) {
                             this.dropAndBlacklistHive();
@@ -995,7 +_,7 @@
                 return true;
             } else {
                 Path path = Bee.this.navigation.getPath();
-                return path != null && path.getTarget().equals(pos) && path.canReach() && path.isDone();
+                return path != null && path.isProcessed() && path.getTarget().equals(pos) && path.canReach() && path.isDone(); // DivineMC - ensure path is processed
             }
         }
     }
@@ -1155,9 +_,27 @@
 
         private List<BlockPos> findNearbyHivesWithSpace() {
             BlockPos blockPos = Bee.this.blockPosition();
-            PoiManager poiManager = ((ServerLevel)Bee.this.level()).getPoiManager();
-            Stream<PoiRecord> inRange = poiManager.getInRange(poiRecord -> poiRecord.is(PoiTypeTags.BEE_HOME), blockPos, 20, PoiManager.Occupancy.ANY);
-            return inRange.map(PoiRecord::getPos)
+            ServerLevel level = (ServerLevel)Bee.this.level();
+            PoiManager poiManager = level.getPoiManager();
+            List<BlockPos> hivePositions = new ArrayList<>();
+            int distance = 20;
+            int maxDistanceSqr = distance * distance;
+            int chunkRadius = Math.floorDiv(distance, 16) + 1;
+
+            ChunkPos.rangeClosed(new ChunkPos(blockPos), chunkRadius)
+                .filter(chunkPos -> TickThread.isTickThreadFor(level, chunkPos))
+                .forEach(chunkPos -> {
+                    for (int sectionY = level.getMinSectionY(); sectionY <= level.getMaxSectionY(); sectionY++) {
+                        poiManager.get(SectionPos.of(chunkPos, sectionY).asLong())
+                            .ifPresent(section -> section.getRecords(poiRecord -> poiRecord.is(PoiTypeTags.BEE_HOME), PoiManager.Occupancy.ANY)
+                                .map(PoiRecord::getPos)
+                                .forEach(hivePositions::add));
+                    }
+                });
+
+            return hivePositions.stream()
+                .filter(pos -> Math.abs(pos.getX() - blockPos.getX()) <= distance && Math.abs(pos.getZ() - blockPos.getZ()) <= distance)
+                .filter(pos -> pos.distSqr(blockPos) <= maxDistanceSqr)
                 .filter(Bee.this::doesHiveHaveSpace)
                 .sorted(Comparator.comparingDouble(pos -> pos.distSqr(blockPos)))
                 .collect(Collectors.toList());
