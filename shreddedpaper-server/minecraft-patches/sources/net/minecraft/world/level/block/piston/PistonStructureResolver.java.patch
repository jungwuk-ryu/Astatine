--- a/net/minecraft/world/level/block/piston/PistonStructureResolver.java
+++ b/net/minecraft/world/level/block/piston/PistonStructureResolver.java
@@ -37,7 +_,10 @@
     public boolean resolve() {
         this.toPush.clear();
         this.toDestroy.clear();
-        BlockState blockState = this.level.getBlockState(this.startPos);
+        BlockState blockState = this.level.getBlockStateIfLoaded(this.startPos); // ShreddedPaper - loaded-only piston probe
+        if (blockState == null) {
+            return false;
+        }
         if (!PistonBaseBlock.isPushable(blockState, this.level, this.startPos, this.pushDirection, false, this.pistonDirection)) {
             if (this.extending && blockState.getPistonPushReaction() == PushReaction.DESTROY) {
                 this.toDestroy.add(this.startPos);
@@ -50,7 +_,8 @@
         } else {
             for (int i = 0; i < this.toPush.size(); i++) {
                 BlockPos blockPos = this.toPush.get(i);
-                if (isSticky(this.level.getBlockState(blockPos)) && !this.addBranchingBlocks(blockPos)) {
+                BlockState pushedState = this.level.getBlockStateIfLoaded(blockPos); // ShreddedPaper - loaded-only piston probe
+                if (pushedState != null && isSticky(pushedState) && !this.addBranchingBlocks(blockPos)) {
                     return false;
                 }
             }
@@ -70,7 +_,10 @@
     }
 
     private boolean addBlockLine(BlockPos originPos, Direction direction) {
-        BlockState blockState = this.level.getBlockState(originPos);
+        BlockState blockState = this.level.getBlockStateIfLoaded(originPos); // ShreddedPaper - loaded-only piston probe
+        if (blockState == null) {
+            return false;
+        }
         if (blockState.isAir()) {
             return true;
         } else if (!PistonBaseBlock.isPushable(blockState, this.level, originPos, this.pushDirection, false, direction)) {
@@ -87,7 +_,10 @@
                 while (isSticky(blockState)) {
                     BlockPos blockPos = originPos.relative(this.pushDirection.getOpposite(), i);
                     BlockState blockState1 = blockState;
-                    blockState = this.level.getBlockState(blockPos);
+                    blockState = this.level.getBlockStateIfLoaded(blockPos); // ShreddedPaper - loaded-only piston probe
+                    if (blockState == null) {
+                        return false;
+                    }
                     if (blockState.isAir()
                         || !canStickToEachOther(blockState1, blockState)
                         || !PistonBaseBlock.isPushable(blockState, this.level, blockPos, this.pushDirection, false, this.pushDirection.getOpposite())
@@ -117,7 +_,8 @@
 
                         for (int i3 = 0; i3 <= index + i1; i3++) {
                             BlockPos blockPos2 = this.toPush.get(i3);
-                            if (isSticky(this.level.getBlockState(blockPos2)) && !this.addBranchingBlocks(blockPos2)) {
+                            BlockState pushedState = this.level.getBlockStateIfLoaded(blockPos2); // ShreddedPaper - loaded-only piston probe
+                            if (pushedState != null && isSticky(pushedState) && !this.addBranchingBlocks(blockPos2)) {
                                 return false;
                             }
                         }
@@ -125,7 +_,10 @@
                         return true;
                     }
 
-                    blockState = this.level.getBlockState(blockPos1);
+                    blockState = this.level.getBlockStateIfLoaded(blockPos1); // ShreddedPaper - loaded-only piston probe
+                    if (blockState == null) {
+                        return false;
+                    }
                     if (blockState.isAir()) {
                         return true;
                     }
@@ -166,12 +_,18 @@
     }
 
     private boolean addBranchingBlocks(BlockPos fromPos) {
-        BlockState blockState = this.level.getBlockState(fromPos);
+        BlockState blockState = this.level.getBlockStateIfLoaded(fromPos); // ShreddedPaper - loaded-only piston probe
+        if (blockState == null) {
+            return false;
+        }
 
         for (Direction direction : Direction.values()) {
             if (direction.getAxis() != this.pushDirection.getAxis()) {
                 BlockPos blockPos = fromPos.relative(direction);
-                BlockState blockState1 = this.level.getBlockState(blockPos);
+                BlockState blockState1 = this.level.getBlockStateIfLoaded(blockPos); // ShreddedPaper - loaded-only piston probe
+                if (blockState1 == null) {
+                    return false;
+                }
                 if (canStickToEachOther(blockState1, blockState) && !this.addBlockLine(blockPos, direction)) {
                     return false;
                 }
