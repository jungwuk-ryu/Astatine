--- a/net/minecraft/world/level/pathfinder/PathFinder.java
+++ b/net/minecraft/world/level/pathfinder/PathFinder.java
@@ -19,6 +_,8 @@
 import net.minecraft.world.level.PathNavigationRegion;
 import org.jspecify.annotations.Nullable;
 
+import org.bxteam.divinemc.async.pathfinding.NodeEvaluatorGenerator; // DivineMC - Petal: Async Pathfinding
+
 public class PathFinder {
     private static final float FUDGING = 1.5F;
     private final Node[] neighbors = new Node[32];
@@ -26,11 +_,19 @@
     public final NodeEvaluator nodeEvaluator;
     private final BinaryHeap openSet = new BinaryHeap();
     private BooleanSupplier captureDebug = () -> false;
+    private final @Nullable NodeEvaluatorGenerator nodeEvaluatorGenerator; // DivineMC - Async Pathfinding
 
-    public PathFinder(NodeEvaluator nodeEvaluator, int maxVisitedNodes) {
+    // DivineMC start - Async Pathfinding
+    public PathFinder(NodeEvaluator nodeEvaluator, int maxVisitedNodes, @Nullable NodeEvaluatorGenerator nodeEvaluatorGenerator) {
         this.nodeEvaluator = nodeEvaluator;
         this.maxVisitedNodes = maxVisitedNodes;
-    }
+        this.nodeEvaluatorGenerator = nodeEvaluatorGenerator;
+    }
+
+    public PathFinder(NodeEvaluator nodeEvaluator, int maxVisitedNodes) {
+        this(nodeEvaluator, maxVisitedNodes, null);
+    }
+    // DivineMC end - Async Pathfinding
 
     public void setCaptureDebug(BooleanSupplier captureDebug) {
         this.captureDebug = captureDebug;
@@ -42,24 +_,92 @@
 
     public @Nullable Path findPath(PathNavigationRegion region, Mob mob, Set<BlockPos> targets, float maxRange, int reachRange, float maxVisitedNodesMultiplier) {
         this.openSet.clear();
-        this.nodeEvaluator.prepare(region, mob);
-        Node start = this.nodeEvaluator.getStart();
+        // DivineMC start - Async Pathfinding
+        NodeEvaluator nodeEvaluator = this.nodeEvaluatorGenerator == null
+            ? this.nodeEvaluator
+            : org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.takeNodeEvaluator(this.nodeEvaluatorGenerator, this.nodeEvaluator);
+        nodeEvaluator.prepare(region, mob);
+        nodeEvaluator.clearCache();
+        Node start = nodeEvaluator.getStart();
         if (start == null) {
+            if (nodeEvaluator != this.nodeEvaluator) {
+                nodeEvaluator.done();
+                org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.removeNodeEvaluator(nodeEvaluator);
+            }
             return null;
-        } else {
-            // Paper start - Perf: remove streams and optimize collection
-            List<Map.Entry<Target, BlockPos>> map = Lists.newArrayList();
-            for (BlockPos pos : targets) {
-                map.add(new java.util.AbstractMap.SimpleEntry<>(this.nodeEvaluator.getTarget(pos.getX(), pos.getY(), pos.getZ()), pos));
-            }
-            // Paper end - Perf: remove streams and optimize collection
+        }
+
+        // Paper start - Perf: remove streams and optimize collection
+        List<Map.Entry<Target, BlockPos>> map = Lists.newArrayList();
+        for (BlockPos pos : targets) {
+            map.add(new java.util.AbstractMap.SimpleEntry<>(nodeEvaluator.getTarget(pos.getX(), pos.getY(), pos.getZ()), pos)); // DivineMC - handle nodeEvaluatorGenerator
+        }
+        // Paper end - Perf: remove streams and optimize collection
+        if (this.nodeEvaluatorGenerator == null) {
             Path path = this.findPath(start, map, maxRange, reachRange, maxVisitedNodesMultiplier);
             this.nodeEvaluator.done();
             return path;
         }
+
+        final int maxVisitedNodes = this.maxVisitedNodes;
+        final BooleanSupplier captureDebug = this.captureDebug;
+        final PathNavigationRegion regionCapture = region;
+        final Mob mobCapture = mob;
+        final NodeEvaluatorGenerator generatorCapture = this.nodeEvaluatorGenerator;
+
+        final int nodeEvaluatorFeatures = org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures.fromNodeEvaluator(nodeEvaluator);
+
+        nodeEvaluator.done();
+        org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.returnNodeEvaluator(nodeEvaluator);
+
+        return new org.bxteam.divinemc.async.pathfinding.AsyncPath(this,Lists.newArrayList(), targets, finder -> {
+            NodeEvaluator asyncEvaluator = generatorCapture.generate(
+                org.bxteam.divinemc.async.pathfinding.NodeEvaluatorFeatures.unpack(nodeEvaluatorFeatures)
+            );
+
+            try {
+                asyncEvaluator.prepare(regionCapture, mobCapture);
+                asyncEvaluator.clearCache();
+
+                Node asyncStart = asyncEvaluator.getStart();
+                if (asyncStart == null) {
+                    return new Path(Lists.newArrayList(), targets.iterator().next(), false);
+                }
+
+                List<Map.Entry<Target, BlockPos>> asyncMap = Lists.newArrayList();
+                for (BlockPos pos : targets) {
+                    asyncMap.add(new java.util.AbstractMap.SimpleEntry<>(
+                        asyncEvaluator.getTarget(pos.getX(), pos.getY(), pos.getZ()), pos
+                    ));
+                }
+
+                return finder.findPath(
+                    asyncEvaluator,
+                    org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.HEAP_LOCAL.get(),
+                    org.bxteam.divinemc.async.pathfinding.NodeEvaluatorCache.NEIGHBORS_LOCAL.get(),
+                    maxVisitedNodes,
+                    captureDebug,
+                    asyncStart,
+                    asyncMap,
+                    maxRange,
+                    reachRange,
+                    maxVisitedNodesMultiplier
+                );
+            } finally {
+                asyncEvaluator.done();
+                asyncEvaluator.clearNodes();
+            }
+        });
+        // DivineMC end - Async Pathfinding
     }
 
+    // DivineMC start - Async Pathfinding
     private @Nullable Path findPath(Node node, List<Map.Entry<Target, BlockPos>> positions, float maxRange, int reachRange, float maxVisitedNodesMultiplier) { // Paper - optimize collection
+        return findPath(this.nodeEvaluator, this.openSet, this.neighbors, this.maxVisitedNodes, this.captureDebug, node, positions, maxRange, reachRange, maxVisitedNodesMultiplier);
+    }
+
+    private Path findPath(NodeEvaluator nodeEvaluator, BinaryHeap openSet, Node[] neighborsArr, int maxVisitedNodes, BooleanSupplier captureDebug, Node node, List<Map.Entry<Target, BlockPos>> positions, float maxRange, int reachRange, float maxVisitedNodesMultiplier) {
+    // DivineMC end - Async Pathfinding
         ProfilerFiller profilerFiller = Profiler.get();
         profilerFiller.push("find_path");
         profilerFiller.markForCharting(MetricCategory.PATH_FINDING);
@@ -67,20 +_,22 @@
         node.g = 0.0F;
         node.h = this.getBestH(node, positions); // Paper - optimize collection
         node.f = node.h;
-        this.openSet.clear();
-        this.openSet.insert(node);
-        boolean asBoolean = this.captureDebug.getAsBoolean();
+        // DivineMC start - Async Pathfinding
+        openSet.clear();
+        openSet.insert(node);
+        boolean asBoolean = captureDebug.getAsBoolean();
+        // DivineMC end - Async Pathfinding
         Set<Node> set1 = asBoolean ? new HashSet<>() : Set.of();
         int i = 0;
         List<Map.Entry<Target, BlockPos>> entryList = Lists.newArrayListWithExpectedSize(positions.size()); // Paper - optimize collection
-        int i1 = (int)(this.maxVisitedNodes * maxVisitedNodesMultiplier);
+        int i1 = (int)(maxVisitedNodes * maxVisitedNodesMultiplier); // DivineMC - Async Pathfinding
 
-        while (!this.openSet.isEmpty()) {
+        while (!openSet.isEmpty()) { // DivineMC - Async Pathfinding
             if (++i >= i1) {
                 break;
             }
 
-            Node node1 = this.openSet.pop();
+            Node node1 = openSet.pop(); // DivineMC - Async Pathfinding
             node1.closed = true;
 
             // Paper start - optimize collection
@@ -103,10 +_,10 @@
             }
 
             if (!(node1.distanceTo(node) >= maxRange)) {
-                int neighbors = this.nodeEvaluator.getNeighbors(this.neighbors, node1);
+                int neighbors = nodeEvaluator.getNeighbors(neighborsArr, node1); // DivineMC - Async Pathfinding
 
                 for (int i2 = 0; i2 < neighbors; i2++) {
-                    Node node2 = this.neighbors[i2];
+                    Node node2 = neighborsArr[i2]; // DivineMC - Async Pathfinding
                     float f = this.distance(node1, node2);
                     node2.walkedDistance = node1.walkedDistance + f;
                     float f1 = node1.g + f + node2.costMalus;
@@ -115,10 +_,10 @@
                         node2.g = f1;
                         node2.h = this.getBestH(node2, positions) * 1.5F; // Paper - Perf: remove streams and optimize collection
                         if (node2.inOpenSet()) {
-                            this.openSet.changeCost(node2, node2.g + node2.h);
+                            openSet.changeCost(node2, node2.g + node2.h); // DivineMC - Async Pathfinding
                         } else {
                             node2.f = node2.g + node2.h;
-                            this.openSet.insert(node2);
+                            openSet.insert(node2); // DivineMC - Async Pathfinding
                         }
                     }
                 }
@@ -143,9 +_,9 @@
             for(Map.Entry<Target, BlockPos> entry : positions) {
                 set.add(entry.getKey());
             }
-            best.setDebug(this.openSet.getHeap(), set1.toArray(Node[]::new), set);
+            best.setDebug(openSet.getHeap(), set1.toArray(Node[]::new), set); // DivineMC - Async Pathfinding
         }
-        return best;
+        return java.util.Objects.requireNonNull(best); // DivineMC - Async Pathfinding
         // Paper end - Perf: remove streams and optimize collection
     }
 
