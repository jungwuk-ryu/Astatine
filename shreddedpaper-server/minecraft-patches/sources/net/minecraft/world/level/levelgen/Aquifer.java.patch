--- a/net/minecraft/world/level/levelgen/Aquifer.java
+++ b/net/minecraft/world/level/levelgen/Aquifer.java
@@ -96,7 +_,15 @@
         private final int minGridY;
         private final int minGridZ;
         private final int gridSizeX;
         private final int gridSizeZ;
+        // ShreddedPaper start - C2ME: Optimize Aquifer
+        private int c2me$packed1;
+        private int c2me$packed2;
+        private int c2me$packed3;
+        private int c2me$packed4;
+        private double c2me$mutableDoubleThingy;
+        private short[] c2me$packedBlockPositions;
+        // ShreddedPaper end - C2ME: Optimize Aquifer
         private static final int[][] SURFACE_SAMPLING_OFFSETS_IN_CHUNKS = new int[][]{
             {0, 0}, {-2, -1}, {-1, -1}, {0, -1}, {1, -1}, {-3, 0}, {-2, 0}, {-1, 0}, {1, 0}, {-2, 1}, {-1, 1}, {0, 1}, {1, 1}
         };
@@ -131,6 +_,40 @@
             this.aquiferCache = new Aquifer.FluidStatus[i4];
             this.aquiferLocationCache = new long[i4];
             Arrays.fill(this.aquiferLocationCache, Long.MAX_VALUE);
+            // ShreddedPaper start - C2ME: Optimize Aquifer
+            if (this.aquiferLocationCache.length % (this.gridSizeX * this.gridSizeZ) != 0) {
+                throw new AssertionError("Array length");
+            }
+
+            int sizeY = this.aquiferLocationCache.length / (this.gridSizeX * this.gridSizeZ);
+            this.c2me$packedBlockPositions = new short[this.aquiferLocationCache.length];
+
+            final RandomSource random = com.ishland.c2me.opts.worldgen.general.common.random_instances.RandomUtils.getRandom(this.positionalRandomFactory);
+            for (int y = 0; y < sizeY; y++) {
+                for (int z = 0; z < this.gridSizeZ; z++) {
+                    for (int x = 0; x < this.gridSizeX; x++) {
+                        final int gridX = x + this.minGridX;
+                        final int gridY = y + this.minGridY;
+                        final int gridZ = z + this.minGridZ;
+                        com.ishland.c2me.opts.worldgen.general.common.random_instances.RandomUtils.derive(this.positionalRandomFactory, random, gridX, gridY, gridZ);
+                        int offsetX = random.nextInt(10);
+                        int offsetY = random.nextInt(9);
+                        int offsetZ = random.nextInt(10);
+                        int blockX = gridX * 16 + offsetX;
+                        int blockY = gridY * 12 + offsetY;
+                        int blockZ = gridZ * 16 + offsetZ;
+                        int index = this.getIndex(gridX, gridY, gridZ);
+                        this.aquiferLocationCache[index] = BlockPos.asLong(blockX, blockY, blockZ);
+                        this.c2me$packedBlockPositions[index] = (short) ((offsetX << 8) | (offsetY << 4) | offsetZ);
+                    }
+                }
+            }
+            for (long blockPosition : this.aquiferLocationCache) {
+                if (blockPosition == Long.MAX_VALUE) {
+                    throw new AssertionError("Array initialization");
+                }
+            }
+            // ShreddedPaper end - C2ME: Optimize Aquifer
             int i5 = this.adjustSurfaceLevel(
                 noiseChunk.maxPreliminarySurfaceLevel(fromGridX(this.minGridX, 0), fromGridZ(this.minGridZ, 0), fromGridX(i, 9), fromGridZ(i3, 9))
             );
@@ -151,142 +_,23 @@
                 this.shouldScheduleFluidUpdate = false;
                 return null;
             } else {
+                // ShreddedPaper start - C2ME: Optimize Aquifer
                 int i = context.blockX();
-                int i1 = context.blockY();
-                int i2 = context.blockZ();
-                Aquifer.FluidStatus fluidStatus = this.globalFluidPicker.computeFluid(i, i1, i2);
-                if (i1 > this.skipSamplingAboveY) {
+                int j = context.blockY();
+                int k = context.blockZ();
+                Aquifer.FluidStatus fluidStatus = this.globalFluidPicker.computeFluid(i, j, k);
+                if (j > this.skipSamplingAboveY) {
                     this.shouldScheduleFluidUpdate = false;
-                    return fluidStatus.at(i1);
-                } else if (fluidStatus.at(i1).is(Blocks.LAVA)) {
+                    return fluidStatus.at(j);
+                } else if (fluidStatus.at(j).is(Blocks.LAVA)) {
                     this.shouldScheduleFluidUpdate = false;
                     return SharedConstants.DEBUG_DISABLE_FLUID_GENERATION ? Blocks.AIR.defaultBlockState() : Blocks.LAVA.defaultBlockState();
                 } else {
-                    int i3 = gridX(i + -5);
-                    int i4 = gridY(i1 + 1);
-                    int i5 = gridZ(i2 + -5);
-                    int i6 = Integer.MAX_VALUE;
-                    int i7 = Integer.MAX_VALUE;
-                    int i8 = Integer.MAX_VALUE;
-                    int i9 = Integer.MAX_VALUE;
-                    int i10 = 0;
-                    int i11 = 0;
-                    int i12 = 0;
-                    int i13 = 0;
-
-                    for (int i14 = 0; i14 <= 1; i14++) {
-                        for (int i15 = -1; i15 <= 1; i15++) {
-                            for (int i16 = 0; i16 <= 1; i16++) {
-                                int i17 = i3 + i14;
-                                int i18 = i4 + i15;
-                                int i19 = i5 + i16;
-                                int index = this.getIndex(i17, i18, i19);
-                                long l = this.aquiferLocationCache[index];
-                                long l1;
-                                if (l != Long.MAX_VALUE) {
-                                    l1 = l;
-                                } else {
-                                    RandomSource randomSource = this.positionalRandomFactory.at(i17, i18, i19);
-                                    l1 = BlockPos.asLong(
-                                        fromGridX(i17, randomSource.nextInt(10)),
-                                        fromGridY(i18, randomSource.nextInt(9)),
-                                        fromGridZ(i19, randomSource.nextInt(10))
-                                    );
-                                    this.aquiferLocationCache[index] = l1;
-                                }
-
-                                int i20 = BlockPos.getX(l1) - i;
-                                int i21 = BlockPos.getY(l1) - i1;
-                                int i22 = BlockPos.getZ(l1) - i2;
-                                int i23 = i20 * i20 + i21 * i21 + i22 * i22;
-                                if (i6 >= i23) {
-                                    i13 = i12;
-                                    i12 = i11;
-                                    i11 = i10;
-                                    i10 = index;
-                                    i9 = i8;
-                                    i8 = i7;
-                                    i7 = i6;
-                                    i6 = i23;
-                                } else if (i7 >= i23) {
-                                    i13 = i12;
-                                    i12 = i11;
-                                    i11 = index;
-                                    i9 = i8;
-                                    i8 = i7;
-                                    i7 = i23;
-                                } else if (i8 >= i23) {
-                                    i13 = i12;
-                                    i12 = index;
-                                    i9 = i8;
-                                    i8 = i23;
-                                } else if (i9 >= i23) {
-                                    i13 = index;
-                                    i9 = i23;
-                                }
-                            }
-                        }
-                    }
-
-                    Aquifer.FluidStatus aquiferStatus = this.getAquiferStatus(i10);
-                    double d = similarity(i6, i7);
-                    BlockState blockState = aquiferStatus.at(i1);
-                    BlockState blockState1 = SharedConstants.DEBUG_DISABLE_FLUID_GENERATION ? Blocks.AIR.defaultBlockState() : blockState;
-                    if (d <= 0.0) {
-                        if (d >= FLOWING_UPDATE_SIMULARITY) {
-                            Aquifer.FluidStatus aquiferStatus1 = this.getAquiferStatus(i11);
-                            this.shouldScheduleFluidUpdate = !aquiferStatus.equals(aquiferStatus1);
-                        } else {
-                            this.shouldScheduleFluidUpdate = false;
-                        }
-
-                        return blockState1;
-                    } else if (blockState.is(Blocks.WATER) && this.globalFluidPicker.computeFluid(i, i1 - 1, i2).at(i1 - 1).is(Blocks.LAVA)) {
-                        this.shouldScheduleFluidUpdate = true;
-                        return blockState1;
-                    } else {
-                        MutableDouble mutableDouble = new MutableDouble(Double.NaN);
-                        Aquifer.FluidStatus aquiferStatus2 = this.getAquiferStatus(i11);
-                        double d1 = d * this.calculatePressure(context, mutableDouble, aquiferStatus, aquiferStatus2);
-                        if (substance + d1 > 0.0) {
-                            this.shouldScheduleFluidUpdate = false;
-                            return null;
-                        } else {
-                            Aquifer.FluidStatus aquiferStatus3 = this.getAquiferStatus(i12);
-                            double d2 = similarity(i6, i8);
-                            if (d2 > 0.0) {
-                                double d3 = d * d2 * this.calculatePressure(context, mutableDouble, aquiferStatus, aquiferStatus3);
-                                if (substance + d3 > 0.0) {
-                                    this.shouldScheduleFluidUpdate = false;
-                                    return null;
-                                }
-                            }
-
-                            double d3 = similarity(i7, i8);
-                            if (d3 > 0.0) {
-                                double d4 = d * d3 * this.calculatePressure(context, mutableDouble, aquiferStatus2, aquiferStatus3);
-                                if (substance + d4 > 0.0) {
-                                    this.shouldScheduleFluidUpdate = false;
-                                    return null;
-                                }
-                            }
-
-                            boolean flag = !aquiferStatus.equals(aquiferStatus2);
-                            boolean flag1 = d3 >= FLOWING_UPDATE_SIMULARITY && !aquiferStatus2.equals(aquiferStatus3);
-                            boolean flag2 = d2 >= FLOWING_UPDATE_SIMULARITY && !aquiferStatus.equals(aquiferStatus3);
-                            if (!flag && !flag1 && !flag2) {
-                                this.shouldScheduleFluidUpdate = d2 >= FLOWING_UPDATE_SIMULARITY
-                                    && similarity(i6, i9) >= FLOWING_UPDATE_SIMULARITY
-                                    && !aquiferStatus.equals(this.getAquiferStatus(i13));
-                            } else {
-                                this.shouldScheduleFluidUpdate = true;
-                            }
-
-                            return blockState1;
-                        }
-                    }
+                    this.c2me$refreshDistPosIdx(i, j, k);
+                    return this.c2me$applyPost(context, substance, j, i, k);
                 }
             }
+            // ShreddedPaper end - C2ME: Optimize Aquifer
         }
 
         @Override
@@ -299,65 +_,27 @@
             return 1.0 - (secondDistance - firstDistance) / 25.0;
         }
 
+        // ShreddedPaper start - C2ME: Optimize Aquifer
         private double calculatePressure(
-            DensityFunction.FunctionContext context, MutableDouble substance, Aquifer.FluidStatus firstFluid, Aquifer.FluidStatus secondFluid
+            DensityFunction.FunctionContext context, MutableDouble substance, Aquifer.FluidStatus fluidLevel, Aquifer.FluidStatus fluidLevel2
         ) {
             int i = context.blockY();
-            BlockState blockState = firstFluid.at(i);
-            BlockState blockState1 = secondFluid.at(i);
-            if ((!blockState.is(Blocks.LAVA) || !blockState1.is(Blocks.WATER)) && (!blockState.is(Blocks.WATER) || !blockState1.is(Blocks.LAVA))) {
-                int abs = Math.abs(firstFluid.fluidLevel - secondFluid.fluidLevel);
+            BlockState blockState = fluidLevel.at(i);
+            BlockState blockState2 = fluidLevel2.at(i);
+            if ((!blockState.is(Blocks.LAVA) || !blockState2.is(Blocks.WATER)) && (!blockState.is(Blocks.WATER) || !blockState2.is(Blocks.LAVA))) {
+                int abs = Math.abs(fluidLevel.fluidLevel - fluidLevel2.fluidLevel);
                 if (abs == 0) {
                     return 0.0;
                 } else {
-                    double d = 0.5 * (firstFluid.fluidLevel + secondFluid.fluidLevel);
-                    double d1 = i + 0.5 - d;
-                    double d2 = abs / 2.0;
-                    double d3 = 0.0;
-                    double d4 = 2.5;
-                    double d5 = 1.5;
-                    double d6 = 3.0;
-                    double d7 = 10.0;
-                    double d8 = 3.0;
-                    double d9 = d2 - Math.abs(d1);
-                    double d11;
-                    if (d1 > 0.0) {
-                        double d10 = 0.0 + d9;
-                        if (d10 > 0.0) {
-                            d11 = d10 / 1.5;
-                        } else {
-                            d11 = d10 / 2.5;
-                        }
-                    } else {
-                        double d10 = 3.0 + d9;
-                        if (d10 > 0.0) {
-                            d11 = d10 / 3.0;
-                        } else {
-                            d11 = d10 / 10.0;
-                        }
-                    }
-
-                    double d10x = 2.0;
-                    double d12;
-                    if (!(d11 < -2.0) && !(d11 > 2.0)) {
-                        double d13 = substance.doubleValue();
-                        if (Double.isNaN(d13)) {
-                            double d14 = this.barrierNoise.compute(context);
-                            substance.setValue(d14);
-                            d12 = d14;
-                        } else {
-                            d12 = d13;
-                        }
-                    } else {
-                        d12 = 0.0;
-                    }
-
-                    return 2.0 * (d12 + d11);
+                    double d = 0.5 * (double)(fluidLevel.fluidLevel + fluidLevel2.fluidLevel);
+                    double q = c2me$getQ(i, d, abs);
+                    return this.c2me$postCalculateDensity(context, substance, q);
                 }
             } else {
                 return 2.0;
             }
         }
+        // ShreddedPaper end - C2ME: Optimize Aquifer
 
         private static int gridX(int x) {
             return x >> 4;
@@ -438,22 +_,21 @@
         }
 
         private int computeSurfaceLevel(int x, int y, int z, Aquifer.FluidStatus fluidStatus, int maxSurfaceLevel, boolean fluidPresent) {
-            DensityFunction.SinglePointContext singlePointContext = new DensityFunction.SinglePointContext(x, y, z);
+            // ShreddedPaper start - C2ME: Optimize Aquifer
+            DensityFunction.SinglePointContext unblendedNoisePos = new DensityFunction.SinglePointContext(x, y, z);
             double d;
             double d1;
-            if (OverworldBiomeBuilder.isDeepDarkRegion(this.erosion, this.depth, singlePointContext)) {
+            if (OverworldBiomeBuilder.isDeepDarkRegion(this.erosion, this.depth, unblendedNoisePos)) {
                 d = -1.0;
                 d1 = -1.0;
             } else {
                 int i = maxSurfaceLevel + 8 - y;
-                int i1 = 64;
-                double d2 = fluidPresent ? Mth.clampedMap((double)i, 0.0, 64.0, 1.0, 0.0) : 0.0;
-                double d3 = Mth.clamp(this.fluidLevelFloodednessNoise.compute(singlePointContext), -1.0, 1.0);
-                double d4 = Mth.map(d2, 1.0, 0.0, -0.3, 0.8);
-                double d5 = Mth.map(d2, 1.0, 0.0, -0.8, 0.4);
-                d = d3 - d5;
-                d1 = d3 - d4;
+                double f = fluidPresent ? Mth.clampedMap((double)i, 0.0, 64.0, 1.0, 0.0) : 0.0;
+                double g = Mth.clamp(this.fluidLevelFloodednessNoise.compute(unblendedNoisePos), -1.0, 1.0);
+                d = g + 0.8 + (f - 1.0) * 1.2;
+                d1 = g + 0.3 + (f - 1.0) * 1.1;
             }
+            // ShreddedPaper end - C2ME: Optimize Aquifer
 
             int i;
             if (d1 > 0.0) {
@@ -484,12 +_,12 @@
         private BlockState computeFluidType(int x, int y, int z, Aquifer.FluidStatus fluidStatus, int surfaceLevel) {
             BlockState blockState = fluidStatus.fluidType;
             if (surfaceLevel <= -10 && surfaceLevel != DimensionType.WAY_BELOW_MIN_Y && fluidStatus.fluidType != Blocks.LAVA.defaultBlockState()) {
-                int i = 64;
-                int i1 = 40;
-                int i2 = Math.floorDiv(x, 64);
-                int i3 = Math.floorDiv(y, 40);
-                int i4 = Math.floorDiv(z, 64);
-                double d = this.lavaNoise.compute(new DensityFunction.SinglePointContext(i2, i3, i4));
+                // ShreddedPaper start - C2ME: Optimize Aquifer
+                int k = x >> 6;
+                int l = Math.floorDiv(y, 40);
+                int m = z >> 6;
+                double d = this.lavaNoise.compute(new DensityFunction.SinglePointContext(k, l, m));
+                // ShreddedPaper end - C2ME: Optimize Aquifer
                 if (Math.abs(d) > 0.3) {
                     blockState = Blocks.LAVA.defaultBlockState();
                 }
@@ -497,5 +_,254 @@
 
             return blockState;
         }
+
+        // ShreddedPaper start - C2ME: Optimize Aquifer
+        private @Nullable BlockState c2me$applyPost(DensityFunction.FunctionContext pos, double density, int y, int x, int z) {
+            Aquifer.FluidStatus fluidLevel = this.c2me$getWaterLevelIndexed(c2me$unpackPackedPosIdx(this.c2me$packed1));
+            double firstSecondSimilarity = similarity(c2me$unpackPackedDist(this.c2me$packed1), c2me$unpackPackedDist(this.c2me$packed2));
+            BlockState blockState = fluidLevel.at(y);
+            BlockState returnedBlockState = SharedConstants.DEBUG_DISABLE_FLUID_GENERATION ? Blocks.AIR.defaultBlockState() : blockState;
+
+            if (firstSecondSimilarity <= 0.0) {
+                if (firstSecondSimilarity >= FLOWING_UPDATE_SIMULARITY) {
+                    Aquifer.FluidStatus secondFluidLevel = this.c2me$getWaterLevelIndexed(c2me$unpackPackedPosIdx(this.c2me$packed2));
+                    this.shouldScheduleFluidUpdate = !fluidLevel.equals(secondFluidLevel);
+                } else {
+                    this.shouldScheduleFluidUpdate = false;
+                }
+                return returnedBlockState;
+            } else if (blockState.is(Blocks.WATER) && this.globalFluidPicker.computeFluid(x, y - 1, z).at(y - 1).is(Blocks.LAVA)) {
+                this.shouldScheduleFluidUpdate = true;
+                return returnedBlockState;
+            } else {
+                this.c2me$mutableDoubleThingy = Double.NaN;
+                Aquifer.FluidStatus secondFluidLevel = this.c2me$getWaterLevelIndexed(c2me$unpackPackedPosIdx(this.c2me$packed2));
+                double e = firstSecondSimilarity * this.c2me$calculateDensityModified(pos, fluidLevel, secondFluidLevel);
+                if (density + e > 0.0) {
+                    this.shouldScheduleFluidUpdate = false;
+                    return null;
+                }
+                return this.c2me$getFinalBlockState(pos, density, firstSecondSimilarity, fluidLevel, secondFluidLevel, returnedBlockState);
+            }
+        }
+
+        private BlockState c2me$getFinalBlockState(
+            DensityFunction.FunctionContext pos,
+            double density,
+            double firstSecondSimilarity,
+            Aquifer.FluidStatus firstFluidLevel,
+            Aquifer.FluidStatus secondFluidLevel,
+            BlockState returnedBlockState
+        ) {
+            Aquifer.FluidStatus thirdFluidLevel = this.c2me$getWaterLevelIndexed(c2me$unpackPackedPosIdx(this.c2me$packed3));
+            int firstDistance = c2me$unpackPackedDist(this.c2me$packed1);
+            int secondDistance = c2me$unpackPackedDist(this.c2me$packed2);
+            int thirdDistance = c2me$unpackPackedDist(this.c2me$packed3);
+            double firstThirdSimilarity = similarity(firstDistance, thirdDistance);
+            if (this.c2me$extractedCheck(pos, density, firstSecondSimilarity, firstFluidLevel, firstThirdSimilarity, thirdFluidLevel)) {
+                return null;
+            }
+
+            double secondThirdSimilarity = similarity(secondDistance, thirdDistance);
+            if (this.c2me$extractedCheck(pos, density, firstSecondSimilarity, secondFluidLevel, secondThirdSimilarity, thirdFluidLevel)) {
+                return null;
+            }
+
+            boolean flag = !firstFluidLevel.equals(secondFluidLevel);
+            boolean flag1 = secondThirdSimilarity >= FLOWING_UPDATE_SIMULARITY && !secondFluidLevel.equals(thirdFluidLevel);
+            boolean flag2 = firstThirdSimilarity >= FLOWING_UPDATE_SIMULARITY && !firstFluidLevel.equals(thirdFluidLevel);
+            if (!flag && !flag1 && !flag2) {
+                Aquifer.FluidStatus fourthFluidLevel = this.c2me$getWaterLevelIndexed(c2me$unpackPackedPosIdx(this.c2me$packed4));
+                this.shouldScheduleFluidUpdate = firstThirdSimilarity >= FLOWING_UPDATE_SIMULARITY
+                    && similarity(firstDistance, c2me$unpackPackedDist(this.c2me$packed4)) >= FLOWING_UPDATE_SIMULARITY
+                    && !firstFluidLevel.equals(fourthFluidLevel);
+            } else {
+                this.shouldScheduleFluidUpdate = true;
+            }
+
+            return returnedBlockState;
+        }
+
+        private boolean c2me$extractedCheck(
+            DensityFunction.FunctionContext pos,
+            double density,
+            double firstSecondSimilarity,
+            Aquifer.FluidStatus fluidLevel,
+            double similarity,
+            Aquifer.FluidStatus otherFluidLevel
+        ) {
+            if (similarity > 0.0) {
+                double pressure = firstSecondSimilarity * similarity * this.c2me$calculateDensityModified(pos, fluidLevel, otherFluidLevel);
+                if (density + pressure > 0.0) {
+                    this.shouldScheduleFluidUpdate = false;
+                    return true;
+                }
+            }
+            return false;
+        }
+
+        private void c2me$refreshDistPosIdx(int x, int y, int z) {
+            int gx = (x - 5) >> 4;
+            int gy = Math.floorDiv(y + 1, 12);
+            int gz = (z - 5) >> 4;
+            int first = Integer.MAX_VALUE;
+            int second = Integer.MAX_VALUE;
+            int third = Integer.MAX_VALUE;
+            int fourth = Integer.MAX_VALUE;
+
+            int ordinal = 12;
+            for (int offY = -1; offY <= 1; ++offY) {
+                int gridY = gy + offY;
+                int gridYBase = gridY * 12;
+                for (int offZ = 0; offZ <= 1; ++offZ) {
+                    int gridZ = gz + offZ;
+                    int gridZBase = gridZ << 4;
+
+                    int ordinal0 = ordinal - 1;
+                    int posIdx0 = this.getIndex(gx, gridY, gridZ);
+                    int position0 = this.c2me$packedBlockPositions[posIdx0];
+                    int dx0 = (gx << 4) + c2me$unpackPackedX(position0) - x;
+                    int dy0 = gridYBase + c2me$unpackPackedY(position0) - y;
+                    int dz0 = gridZBase + c2me$unpackPackedZ(position0) - z;
+                    int dist0 = dx0 * dx0 + dy0 * dy0 + dz0 * dz0;
+                    int packed0 = (dist0 << 20) | (ordinal0 << 16) | posIdx0;
+                    if (packed0 <= fourth) {
+                        int carry0 = Math.max(first, packed0);
+                        first = Math.min(first, packed0);
+                        int carry1 = Math.max(second, carry0);
+                        second = Math.min(second, carry0);
+                        int carry2 = Math.max(third, carry1);
+                        third = Math.min(third, carry1);
+                        fourth = Math.min(fourth, carry2);
+                    }
+
+                    int ordinal1 = ordinal - 2;
+                    int posIdx1 = posIdx0 + 1;
+                    int position1 = this.c2me$packedBlockPositions[posIdx1];
+                    int dx1 = ((gx + 1) << 4) + c2me$unpackPackedX(position1) - x;
+                    int dy1 = gridYBase + c2me$unpackPackedY(position1) - y;
+                    int dz1 = gridZBase + c2me$unpackPackedZ(position1) - z;
+                    int dist1 = dx1 * dx1 + dy1 * dy1 + dz1 * dz1;
+                    int packed1 = (dist1 << 20) | (ordinal1 << 16) | posIdx1;
+                    if (packed1 <= fourth) {
+                        int carry0 = Math.max(first, packed1);
+                        first = Math.min(first, packed1);
+                        int carry1 = Math.max(second, carry0);
+                        second = Math.min(second, carry0);
+                        int carry2 = Math.max(third, carry1);
+                        third = Math.min(third, carry1);
+                        fourth = Math.min(fourth, carry2);
+                    }
+
+                    ordinal -= 2;
+                }
+            }
+
+            this.c2me$packed1 = first;
+            this.c2me$packed2 = second;
+            this.c2me$packed3 = third;
+            this.c2me$packed4 = fourth;
+        }
+
+        private double c2me$calculateDensityModified(
+            DensityFunction.FunctionContext pos, Aquifer.FluidStatus fluidLevel, Aquifer.FluidStatus fluidLevel2
+        ) {
+            int i = pos.blockY();
+            BlockState blockState = fluidLevel.at(i);
+            BlockState blockState2 = fluidLevel2.at(i);
+            if ((!blockState.is(Blocks.LAVA) || !blockState2.is(Blocks.WATER)) && (!blockState.is(Blocks.WATER) || !blockState2.is(Blocks.LAVA))) {
+                int j = Math.abs(fluidLevel.fluidLevel - fluidLevel2.fluidLevel);
+                if (j == 0) {
+                    return 0.0;
+                } else {
+                    double d = 0.5 * (double)(fluidLevel.fluidLevel + fluidLevel2.fluidLevel);
+                    double q = c2me$getQ(i, d, j);
+                    return this.c2me$postCalculateDensityModified(pos, q);
+                }
+            } else {
+                return 2.0;
+            }
+        }
+
+        private double c2me$postCalculateDensity(DensityFunction.FunctionContext pos, MutableDouble mutableDouble, double q) {
+            double barrier;
+            if (!(q < -2.0) && !(q > 2.0)) {
+                double cached = mutableDouble.getValue();
+                if (Double.isNaN(cached)) {
+                    barrier = this.barrierNoise.compute(pos);
+                    mutableDouble.setValue(barrier);
+                } else {
+                    barrier = cached;
+                }
+            } else {
+                barrier = 0.0;
+            }
+
+            return 2.0 * (barrier + q);
+        }
+
+        private double c2me$postCalculateDensityModified(DensityFunction.FunctionContext pos, double q) {
+            double barrier;
+            if (!(q < -2.0) && !(q > 2.0)) {
+                double cached = this.c2me$mutableDoubleThingy;
+                if (Double.isNaN(cached)) {
+                    barrier = this.barrierNoise.compute(pos);
+                    this.c2me$mutableDoubleThingy = barrier;
+                } else {
+                    barrier = cached;
+                }
+            } else {
+                barrier = 0.0;
+            }
+
+            return 2.0 * (barrier + q);
+        }
+
+        private static double c2me$getQ(double y, double averageFluidLevel, double fluidLevelDelta) {
+            double offsetFromAverage = y + 0.5 - averageFluidLevel;
+            double halfDelta = fluidLevelDelta / 2.0;
+            double distanceToEdge = halfDelta - Math.abs(offsetFromAverage);
+            double q;
+            if (offsetFromAverage > 0.0) {
+                if (distanceToEdge > 0.0) {
+                    q = distanceToEdge / 1.5;
+                } else {
+                    q = distanceToEdge / 2.5;
+                }
+            } else {
+                double belowOffset = 3.0 + distanceToEdge;
+                if (belowOffset > 0.0) {
+                    q = belowOffset / 3.0;
+                } else {
+                    q = belowOffset / 10.0;
+                }
+            }
+            return q;
+        }
+
+        private Aquifer.FluidStatus c2me$getWaterLevelIndexed(int index) {
+            return this.getAquiferStatus(index);
+        }
+
+        private static int c2me$unpackPackedX(int packed) {
+            return packed >> 8;
+        }
+
+        private static int c2me$unpackPackedY(int packed) {
+            return (packed >> 4) & 0b1111;
+        }
+
+        private static int c2me$unpackPackedZ(int packed) {
+            return packed & 0b1111;
+        }
+
+        private static int c2me$unpackPackedDist(int packed) {
+            return packed >> 20;
+        }
+
+        private static int c2me$unpackPackedPosIdx(int packed) {
+            return packed & 0xffff;
+        }
+        // ShreddedPaper end - C2ME: Optimize Aquifer
     }
 }
